# Migration notes — retrotekt-web-2 → retrotektwebnew

**Date:** 2026-05-20
**Source baseline:** `H:\New folder\retrotekt-web-2\` (master, 6 uncommitted modified files included verbatim — see source `git status`)
**Goal:** production-ready copy with no visual or behavioral change.

## What was removed

### Files / directories not copied
| Path | Reason |
|---|---|
| `node_modules/` | regenerated by `npm install` |
| `.next/` | regenerated by `next build` |
| `.git/` | new tree gets its own git history |
| `.ms-playwright/` | Playwright artifacts (~417 MB) |
| `test-results/` | Playwright artifacts |
| `tmp/hero-mobile-draco-test.glb` | scratch artifact, no references |
| `_perf/baseline.md` | perf scratch note, no references |
| `tsconfig.tsbuildinfo` | incremental-compile cache |
| `.claude/` | Claude Code config, not part of the deployable site |

### Config changes
| File | Change |
|---|---|
| `.env.example` | removed `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` — Sentry was removed from the project; zero source references remain. |

### Source code changes
**None.** All `app/`, `components/`, `lib/`, `hooks/`, `scripts/`, `e2e/`, `public/` files are byte-identical to the source. Visual and behavioral parity is preserved at the file level.

## TestSprite

No references anywhere in the repo. Nothing was removed because nothing was present.

## Sentry

Already absent from `package.json`, source code, and config — only stale entries in `.env.example` survived. Those are now removed.

## Verification gates passed

```
[x] 1. npm install                  — 570 packages, 0 errors (deprecation warnings from transitive deps only)
[x] 2. npx tsc --noEmit             — 0 errors
[x] 3. npm run lint                 — 0 errors, 0 warnings
[x] 4. npm test (vitest)            — 28/28 passed
[x] 5. npm run build                — exit 0, 14 routes generated
        / (16.4 kB, 115 kB FLJS)
        /_not-found
        /api/contact (dynamic)
        /consulting (6.75 kB)
        /icon.png
        /portfolio (4.7 kB)
        /portfolio/[slug] × 3 (chocolate-fish-modesto, -livermore, -sacramento)
        /robots.txt
        /services (4.34 kB)
        /sitemap.xml
[ ] 6. npm start + browser visual check  — pending user verification
```

## Source diff (machine-verifiable)

robocopy file-level diff of `app/`, `components/`, `lib/`, `hooks/` between source and new tree: **identical**.

Only top-level differences:
- new tree has: `KILL_LIST.md`, `MIGRATION_NOTES.md`, `MEGA_PLAN.md`
- new tree's `.env.example` is shorter (Sentry block removed)

## Purge pass (applied after baseline)

After baseline parity was proven, ran an evidence-based source-purge pass. All removals had grep-confirmed zero usage outside the deleted site.

| Removal | Evidence | Effect |
|---|---|---|
| `components/DebugPanel.tsx` (deleted) | grep `DebugPanel` across source returned only the def site + its sole import in `app/HomeClient.tsx`. Was dead-code-eliminated in prod via `IS_PROD` guard. | Source surface −65 lines. Bundle unchanged (already DCE'd). |
| `DebugPanel` import + JSX in `app/HomeClient.tsx` | Followed from above. | Source surface −2 lines. |
| `@testing-library/react` (devDependency) | grep across source/tests/configs returned only `package.json`. Tests don't render React. | 17 transitive packages dropped on `npm install`. |

### Post-purge verification
```
[x] npx tsc --noEmit             — 0 errors
[x] npm run lint                 — 0 errors
[x] npm test (vitest)            — 28/28 passed
[x] npm run build                — exit 0, 14 routes, identical byte sizes to pre-purge build
[x] curl every route             — same status codes (200/308/404)
[x] homepage HTML size           — 135351 bytes — IDENTICAL to pre-purge
[x] security headers intact      — CSP, HSTS, X-Frame-Options, etc.
[x] "DebugPanel" in served HTML  — 0 hits (confirmed gone)
```

The byte-identical homepage HTML proves the purge had **zero observable effect** on the rendered output.

## Audited and intentionally kept

- `@testing-library/jest-dom` — referenced by `vitest.setup.ts` (harmless devDep).
- `@vitejs/plugin-react`, `jsdom` — referenced by `vitest.config.ts`.
- `critters` — transitive runtime dep of Next.js `experimental.optimizeCss`.
- `playwright` — peer of `@playwright/test`, used by `playwright.config.ts` + `e2e/*.spec.ts`.
- `tsx` — runs every `build:*` script in `package.json`.
- `public/showcase/*.avif` — produced by `scripts/build-showcase.ts`; pipeline asset.
- Type exports `ProjectAssets`, `ProjectSlug`, `ProcessCardProps`, `ServiceCardProps` — internal-only but stripping `export` keyword is TS-only churn with zero runtime effect.

## Still deferred

1. **Split test deps into a separate install group** — `playwright`, vitest, etc. are large devDeps. Only worth doing if CI image size matters.

## Rollback

The source repo at `H:\New folder\retrotekt-web-2\` is untouched. To roll back the production cut, simply continue deploying from the source repo and discard `H:\New folder\retrotektwebnew\`. New tree's baseline commit is at git SHA `a82449a`.

## Next steps for the operator

1. Boot the new tree: `cd "H:\New folder\retrotektwebnew" && npm start` (after `npm run build`).
2. Open http://localhost:3000 and check `/`, `/services`, `/portfolio`, `/portfolio/chocolate-fish-modesto`, `/consulting`.
3. Verify no console errors, no failed network requests, hero 3D renders.
4. If parity proven: tag `v1.0.0` and proceed with deployment (Vercel via existing `vercel.json`).
5. If issues: source tree at `retrotekt-web-2/` is the rollback.
