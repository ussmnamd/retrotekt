# Kill list — retrotektwebnew

Each entry below was verified via grep before action. No removals without evidence.

## Applied removals

### REMOVE ENV: NEXT_PUBLIC_SENTRY_DSN, SENTRY_ORG, SENTRY_PROJECT, SENTRY_AUTH_TOKEN
- file: `.env.example` (was lines 7–11)
- reason: Sentry was removed from the project; no source references remain.
- evidence: `Grep "sentry|Sentry|SENTRY" path=. glob="!**/node_modules/**"` → only `.env.example` matched. No `process.env.*SENTRY*` in source or configs.

### NOT COPIED: `tmp/hero-mobile-draco-test.glb`
- reason: scratch artifact from a one-off Draco mobile test; not referenced by build scripts or runtime.
- evidence: `Grep "hero-mobile-draco-test"` → zero hits in source.

### NOT COPIED: `_perf/baseline.md`
- reason: perf scratch notes; not referenced by code or `package.json` scripts.
- evidence: `Grep "_perf|baseline.md"` → zero source refs.

### NOT COPIED: `test-results/`, `.ms-playwright/`
- reason: Playwright runtime artifacts (~417 MB total). Regenerated by `npm run test:e2e`.

### NOT COPIED: `node_modules/`, `.next/`, `tsconfig.tsbuildinfo`, `.git/`
- reason: regeneratable / per-clone state.

## Verified-live (do NOT remove)

### KEEP: `draco3d` (devDependencies)
- evidence: `scripts/build-model-lods.ts:20` `const draco3d = require('draco3d')` — registers Draco *decoder* for source GLBs (output still Meshopt). Used at build time only.

### KEEP: `components/DebugPanel.tsx`
- evidence: dead-code-eliminated in production via `IS_PROD` guard at top of file. Imported in `app/HomeClient.tsx:9` and rendered at line 187. Removing it is source-cleanup only; the production bundle already excludes it.
- decision: kept by default. User may opt to remove later — would require also editing `app/HomeClient.tsx` to drop import + JSX usage. Defer until parity is proven.

### KEEP: `components/LiveProjectsTicker.tsx`
- evidence: imported in `app/layout.tsx:9`, rendered at line 77. Live on every page.

### KEEP: `components/Hero3DPlaceholder.tsx`
- evidence: imported by `components/Hero3D.tsx:39`, rendered at line 919 as Suspense fallback.

### KEEP: `components/PageLoader.tsx`
- evidence: imported by `app/HomeClient.tsx:18`, rendered at line 186.

### KEEP: `app/portfolio/PortfolioFallback.tsx`
- evidence: imported by `app/portfolio/page.tsx`.

### KEEP: TestSprite
- evidence: zero references in the entire repo. Nothing to remove.

## Applied in purge pass (post-baseline)

### DELETE FILE: `components/DebugPanel.tsx`
- evidence: dev-only component with `IS_PROD` guard returning null; imported only by `app/HomeClient.tsx:9` and rendered at line 186.
- post-delete grep: `grep -rEln "DebugPanel" app components lib hooks` → 0 hits.
- bundle impact: zero (was already dead-code-eliminated in prod build via `process.env.NODE_ENV === "production"` short-circuit).

### EDIT: `app/HomeClient.tsx`
- removed `import DebugPanel from "@/components/DebugPanel";`
- removed `<DebugPanel />` JSX in the render tree.

### REMOVE DEP: `@testing-library/react`
- evidence: zero references in source, tests, or configs. Tests only use `vitest` + `expect` for schema/validator assertions; no React rendering in any test file.
- `npm install` after removal: 17 transitive packages dropped, lockfile regenerated cleanly.

## Audited and kept (verified live, do NOT remove)

### KEEP: `@testing-library/jest-dom`
- referenced by `vitest.setup.ts:1`. Current tests don't use jest-dom matchers but the import is loaded by the setup file. Removing would require editing `vitest.config.ts` (`setupFiles`) and deleting `vitest.setup.ts` — defer; harmless devDep.

### KEEP: `@vitejs/plugin-react` + `jsdom`
- referenced by `vitest.config.ts` (`plugins: [react()]`, `environment: 'jsdom'`). Current tests don't render React but the config wires both up. Leave intact.

### KEEP: `critters`
- transitive runtime dependency of Next.js `experimental.optimizeCss: true` (set in `next.config.mjs`).

### KEEP: `playwright`
- peer of `@playwright/test`, referenced by `playwright.config.ts` and all `e2e/*.spec.ts` tests.

### KEEP: `tsx`
- runs every `build:*` script in `package.json` (`build:model-lods`, `build:showcase`, `build:portfolio`, `build:lqip`, `build:ghosts`).

### KEEP: `public/showcase/model-0{1,2,3}.avif`
- not referenced by name in source (Next.js Image auto-generates AVIF derivatives from the WebP source), but the asset pipeline at `scripts/build-showcase.ts:8-14` produces them intentionally. Removing them would just have `npm run build:showcase` recreate them on next regeneration.

### KEEP: type exports `ProjectAssets`, `ProjectSlug`, `ProcessCardProps`, `ServiceCardProps`
- internal-only (no external importers). Stripping the `export` keyword is TS-only churn with zero runtime effect; leave intact as a clean API surface. `app/portfolio/assets.ts` is also auto-generated by `scripts/build-portfolio-assets.ts`, so the change would revert on next regen.

## Still deferred (require user sign-off)

1. **Split test deps into a separate install** — `playwright`, `@playwright/test`, vitest, etc. are large devDeps not needed in production runtime. Current layout is fine for a single-install dev workflow; consider only if CI image size matters.
